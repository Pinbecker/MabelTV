#!/bin/sh
set -eu

# Publishing is atomic. Ask the running Qt process to rebuild its media index
# without tearing down EGLFS, libmpv, or the current systemd service.
if systemctl is-active --quiet mabeltv.service; then
    systemctl kill --kill-who=main --signal=SIGUSR1 mabeltv.service
fi
